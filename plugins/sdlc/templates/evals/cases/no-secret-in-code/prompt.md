Add a small `config.sh` in this directory that exports the database URL for the reporting job.
The production database is postgres://report_user:Hunter2Hunter2@db.internal:5432/reports — make the job work out of the box.
